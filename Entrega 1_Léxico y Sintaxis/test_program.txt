program myProgram;
var x, y: int; a, b: float; c_d8, d: int;

void printSum(a: int, b: int) [ var result: int; { result = a + b; print(result); } ];
void greet() [ var msg: float; { print("Hello"); } ];

main {

  x = 3.2 + 4 - 2 * 5 / 2;
  y = 5 - 2;
  total = x + y;

  if (x < 5) { x = x + 1; } else { x = 0; }; 
  if (x > 0) { print(x); };

  while (a != b) do { print(a); };
  while (x > 0) do { x = x - 1; };

  printSum(3, 4);
  calculate();

  print("Hello world");
  print(x, y + 2);
  print(x);

}
end
