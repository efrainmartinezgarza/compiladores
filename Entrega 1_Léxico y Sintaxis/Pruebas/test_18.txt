program Simple;
var
  x : int;

void SquareAndCheck(num : int) [
  var
    sq : int;
  {
    sq = num * num;
    if (sq > 50) {
      print("Grande: ", sq);
    } else {
      print("Pequenio: ", sq);
    };
  }
];

main {
  x = 5;
  SquareAndCheck(x);
  x = 3;
  SquareAndCheck(x);
}
end
